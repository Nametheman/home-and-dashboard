import { VerifyRegisterComponent } from 'components/Auth';
import React from 'react';
import { HomeLayout } from '../../layout';

const Login = () => {
  return <HomeLayoutcontent={<VerifyRegisterComponent />} />;
};

export default Login;
